# Local dev – talk to backend directly
VITE_API_URL=http://localhost:4000/api

# Production (Vercel): leave unset or set to /api (proxied via vercel.json to Railway)
# VITE_API_URL=/api

# Admin login is via backend; optional display name
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=admin123
VITE_ADMIN_NAME=Admin User
